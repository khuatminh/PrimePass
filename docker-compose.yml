services:
  app:
    build: .
    container_name: marketplace-app
    restart: unless-stopped
    environment:
      - DB_HOST=mysql
      - DB_NAME=digital_marketplace
      - DB_USER=marketplace
      - DB_PASS=${DB_PASSWORD:-changeme}
      - VNPAY_TMN_CODE=${VNPAY_TMN_CODE:-DEMO0001}
      - VNPAY_HASH_SECRET=${VNPAY_HASH_SECRET:-DEMOHASHDEMOHASHDEMOHASHDEMO1234}
      - VNPAY_RETURN_URL=${VNPAY_RETURN_URL:-http://localhost/payment/callback}
    volumes:
      - uploads:/app/uploads
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8.0
    container_name: marketplace-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: digital_marketplace
      MYSQL_USER: marketplace
      MYSQL_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - mysql-data:/var/lib/mysql
      - ./database/digital_marketplace.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10

  nginx:
    image: nginx:alpine
    container_name: marketplace-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app

volumes:
  mysql-data:
  uploads:
